const User = require('./User');
const Thought = require('./Thought');
const Reaction = require('./Reaction');  // If Reaction is used directly, otherwise it can be omitted

module.exports = { User, Thought, Reaction };